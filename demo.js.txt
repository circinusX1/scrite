
demo := Demo ();   // class demo, the class in in C++

function main(param)
{
    print("SETUP CALLED");

    demo.Method(4,"test");  // call class method for demo purposes.

    run(loop,1000);     // call back function
    return true;
}

var j=10;
function loop(msecs)
{
    print("LOOP CALLED AT" + msecs + "\n");
    return j--;
}
